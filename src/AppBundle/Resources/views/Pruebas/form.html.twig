<h2>Formulario con Symfony 3</h2>
{% if (status != null) and (data != null) %}
  <h3>{{status}}</h3>
  {{ dump(data) }}
{% endif %}

{{form_start(form1,{'action':'','method':'POST'})}}
  {{form_errors(form1.titulo)}}
  {{form_widget(form1.titulo)}}
{{form_end(form1)}}
